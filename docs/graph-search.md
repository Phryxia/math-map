# 그래프 탐색

# 개요

그래프 탐색은 한 정점에서 시작해 간선을 따라 도달 가능한 정점을 모두 방문하는 절차다. 방문 대기 정점을 스택에 넣으면 깊이 우선 탐색(depth-first search, DFS), 큐에 넣으면 너비 우선 탐색(breadth-first search, BFS) 이고, 두 알고리즘의 차이는 자료구조 하나뿐이다.

탐색이 남기는 트리와 간선의 분류가 그래프의 구조를 드러낸다. 연결성분, 이분성, 순환의 존재, 강연결성분, 가중치 없는 최단경로가 모두 한 번의 탐색으로 나온다.

# 직관

탐색은 정점을 세 상태로 나눈다. 아직 보지 못한 정점, 보았지만 이웃을 다 살피지 않은 정점, 다 살핀 정점이다. 두 번째 무리를 어떤 순서로 꺼내느냐가 탐색의 이름을 정한다. 가장 최근에 본 것을 먼저 꺼내면 한 방향으로 깊이 파고들고, 가장 먼저 본 것을 꺼내면 시작점에서 가까운 순으로 퍼진다.

# 정의

[그래프](graphs.md) $G=(V,E)$ 와 시작 정점 $s$ 에 대해, 탐색은 방문 표시 집합과 대기 컨테이너를 유지하며 다음을 반복한다. 컨테이너에서 정점 $v$ 를 꺼내고, $v$ 의 방문하지 않은 이웃을 방문 표시한 뒤 컨테이너에 넣는다.

컨테이너가 스택이면 **깊이 우선 탐색**, 큐이면 **너비 우선 탐색**이다. 인접 리스트 표현에서 두 탐색 모두 $O(|V|+|E|)$ 시간과 $O(|V|)$ 공간이 든다.

## 탐색 트리와 간선 분류

탐색은 방문한 정점들 위에 트리를 남긴다. 처음 방문할 때 쓴 간선이 트리 간선이고, 나머지는 방문 시각과 종료 시각의 대소로 분류된다. 방향그래프의 DFS 에서 간선 $(u,v)$ 는 네 종류다.

| 종류 | 조건 |
| --- | --- |
| 트리 간선 | $v$ 를 $u$ 에서 처음 방문 |
| 후향 간선 | $v$ 가 $u$ 의 조상, 곧 $v$ 가 아직 종료되지 않음 |
| 전향 간선 | $v$ 가 $u$ 의 후손이고 이미 종료됨 |
| 교차 간선 | 그 밖의 경우 |

# 성질

## BFS 와 최단경로

가중치가 없는 그래프에서 BFS 가 계산한 $\mathrm{dist}(v)$ 는 $s$ 에서 $v$ 까지의 간선 수 최솟값이다. 큐에서 꺼내는 정점의 거리가 비감소한다는 불변량을 귀납법으로 보이면 된다. 거리 $d$ 인 정점이 모두 큐에 들어간 뒤에야 거리 $d+1$ 인 정점이 들어간다.

## 후향 간선과 순환

방향그래프에 순환이 있는 것과 DFS 에서 후향 간선이 나오는 것은 동치다. 후향 간선이 있으면 트리 경로와 합쳐 순환이 만들어지고, 순환이 있으면 그 순환에서 가장 먼저 방문된 정점이 나머지의 조상이 되어 순환을 닫는 간선이 후향 간선이 된다. [위상정렬](dag-topological.md)이 가능한지 판정하는 기준이 이것이다.

## 강연결성분

방향그래프의 강연결성분은 두 번의 DFS 로 구한다. 첫 DFS 로 종료 시각을 구하고, 간선을 뒤집은 그래프에서 종료 시각의 역순으로 다시 DFS 하면 각 탐색 트리가 정확히 하나의 강연결성분이 된다(Kosaraju). 한 번의 DFS 로 같은 일을 하는 Tarjan 알고리즘은 각 정점에서 도달 가능한 최소 방문 시각을 갱신한다.

## 이분성 판정

BFS 로 거리의 홀짝에 따라 두 색을 칠하고, 같은 색 사이에 간선이 있는지 본다. 간선이 있으면 홀수 길이 순환이 존재하고, 없으면 이분그래프다. 그래프가 이분인 것과 홀수 길이 순환이 없는 것이 동치라는 정리의 구성적 증명이다.

# 활용

## 다른 알고리즘의 내부 절차

[최소 신장트리](minimum-spanning-tree.md), [네트워크 흐름](network-flow.md)의 증가경로 찾기, [매칭](matchings.md)의 증대경로 찾기가 모두 탐색을 내부 절차로 쓴다. 흐름 알고리즘에서 증가경로를 BFS 로 고르면 Edmonds–Karp 가 되어 반복 횟수가 다항식으로 억제된다.

## 상태공간 탐색

퍼즐이나 게임의 국면을 정점, 한 수를 간선으로 보면 최소 수 풀이가 BFS 의 최단경로다. 상태 수가 많으면 양방향 탐색이나 휴리스틱을 더한 $A^\ast$ 로 넓히는 범위를 줄인다.

## 무작위 그래프의 성분

[무작위 걷기](random-walks.md)와 달리 탐색은 결정적이지만, 무작위 그래프에서 탐색이 훑는 정점 수를 분지과정으로 근사하면 거대 연결성분의 문턱을 얻는다.

# 연관 문서

## 선수지식

- [그래프](graphs.md)

## 더 알아보기

- [DAG와 위상정렬](dag-topological.md)

#graph_theory #algorithms
