# 최소 신장트리

# 개요

모든 거점을 연결하되 설치 비용을 최소로 하려면 어떤 선을 깔아야 하는가. 선택지가 지수적으로 많은 조합 최적화 문제인데, 답은 놀랄 만큼 단순하다. 가벼운 간선부터 보면서 순환을 만들지 않으면 넣는다. 이 탐욕적 절차가 언제나 최적해를 준다.

탐욕법이 최적을 보장하는 경우는 드물다. 왜 여기서는 통하는가. 이유가 [matroid](matroids.md) 구조에 있다. 순환 없는 간선 집합들이 이루는 족이 교환 성질을 만족하므로 탐욕법의 최적성이 보장되고, 최소 신장트리는 그래프 matroid 의 최소 가중치 기저를 찾는 문제가 된다. 구현에서는 "순환을 만드는가" 를 판정해야 하고 그 일을 [서로소 집합 자료구조](union-find.md)가 맡는다.

# 직관

## 컷을 건너는 가장 가벼운 간선

정점을 두 덩어리로 자르면 두 덩어리를 잇는 간선들이 생긴다. 그중 가장 가벼운 간선은 반드시 어떤 최소 신장트리에 들어간다. 들어가지 않은 최소 신장트리가 있다면, 그 트리에서 같은 컷을 건너는 다른 간선을 빼고 이것을 넣어도 여전히 연결되며 비용은 줄거나 같기 때문이다.

이 컷 성질이 모든 최소 신장트리 알고리즘의 근거다. Kruskal 은 간선을 가벼운 순서로 보면서 암묵적으로 컷을 고르고, Prim 은 지금까지 자란 덩어리와 나머지 사이의 컷을 명시적으로 쓴다. 둘은 같은 정리의 다른 구현이다.

## 최단 경로와 다르다

두 목표를 혼동하기 쉽다. 삼각형에서 `AB = 2`, `BC = 2`, `AC = 3` 이면 최소 신장트리는 `AB` 와 `BC` 로 총 `4` 다. 그런데 `A` 에서 각 정점까지의 최단 경로를 보존하려면 `AB` 와 `AC` 가 필요하고 총 `5` 다.

최소 신장트리는 전체 설치비를 줄이고 최단 경로 트리는 특정 출발점에서의 이동 시간을 줄인다. 목표가 다르므로 답도 다르다. 실무에서 "네트워크를 최적화한다" 고 할 때 어느 쪽인지 먼저 정해야 한다.

# 정의

## 문제

`G = (V, E)` 를 정점이 하나 이상인 유한 연결 무방향 그래프, `w(e)` 를 간선의 실수 가중치라 하자. 신장트리 `T` 는 모든 정점을 포함하고 연결되며 순환이 없는 간선 부분집합이고, 최소 신장트리는

$$
w(T)=\sum_{e\in T}w(e)
$$

를 최소화한다. 가중치에 음수와 동률을 허용한다. 신장트리는 항상 간선 `|V| - 1` 개를 가지므로 음수 가중치가 있어도 문제가 무너지지 않는다. 최단 경로에서 음수 간선이 문제를 일으키는 것과 대조적이다.

그래프가 연결되지 않으면 신장트리가 없고, 각 연결 성분의 최소 신장트리를 모은 최소 신장숲을 구한다.

# 성질

## 컷 성질과 순환 성질

- **컷 성질.** 임의의 컷에서 가중치가 유일하게 최소인 간선은 모든 최소 신장트리에 속한다.
- **순환 성질.** 임의의 순환에서 가중치가 유일하게 최대인 간선은 어떤 최소 신장트리에도 속하지 않는다.

앞의 것이 간선을 넣는 근거, 뒤의 것이 버리는 근거다. 두 성질만으로 알려진 모든 최소 신장트리 알고리즘이 정당화된다.

## Kruskal 알고리즘의 정당성

간선을 가벼운 순서로 훑어 이미 선택한 간선과 순환을 만들지 않을 때만 추가한다. 연결 그래프에서는 `|V| - 1` 개를 선택하면 끝난다.

교환 논증으로 최적성을 본다. 현재 선택한 숲 $F$ 를 포함하는 최소 신장트리 $T$ 가 있다고 하자. 다음 간선 $e$ 가 $T$ 에 없으면 $T \cup \{e\}$ 에 유일한 순환이 생기고, 그 순환에는 $e$ 의 한쪽 끝이 속한 $F$ 의 성분을 벗어나는 다른 간선 $f$ 가 있다. $f$ 는 $F$ 에 없고 선택 가능한 후보였으므로 $w(e) \le w(f)$ 다. $T$ 에서 $f$ 를 빼고 $e$ 를 넣어도 신장트리이고 비용이 늘지 않는다. $F = \emptyset$ 에서 시작해 이 논증을 반복하면 된다.

## 유일성

가중치가 모두 다르면 최소 신장트리는 유일하다. 두 개가 있다면 한쪽에만 있는 가장 가벼운 간선을 잡아 교환 논증으로 모순을 얻는다.

동률이 있으면 여러 개일 수 있지만 동률 자체가 여러 해를 보장하지는 않는다. 동률인 두 간선이 같은 순환에 들어가지 않으면 둘 다 선택되어 해가 하나뿐일 수 있다.

```python
class DSU:
    def __init__(self, n):
        self.p = list(range(n))
    def find(self, x):
        while self.p[x] != x:
            self.p[x] = self.p[self.p[x]]
            x = self.p[x]
        return x
    def union(self, a, b):
        ra, rb = self.find(a), self.find(b)
        if ra == rb:
            return False
        self.p[rb] = ra
        return True


def kruskal(n, edges):
    """edges: (w, u, v) 목록. (총 가중치, 선택된 간선) 반환."""
    d = DSU(n)
    total, chosen = 0, []
    for w, u, v in sorted(edges):
        if d.union(u, v):
            total += w
            chosen.append((w, u, v))
    return total, chosen


# A=0, B=1, C=2
tri = [(2, 0, 1), (2, 1, 2), (3, 0, 2)]
print(kruskal(3, tri))                       # (4, [(2,0,1), (2,1,2)])

# 최단 경로 트리는 다른 답을 준다
def dijkstra_tree(n, edges, src):
    import heapq
    adj = [[] for _ in range(n)]
    for w, u, v in edges:
        adj[u].append((w, v))
        adj[v].append((w, u))
    dist = [float("inf")] * n
    dist[src] = 0
    par = [None] * n
    pq = [(0, src)]
    while pq:
        d0, u = heapq.heappop(pq)
        if d0 > dist[u]:
            continue
        for w, v in adj[u]:
            if d0 + w < dist[v]:
                dist[v] = d0 + w
                par[v] = (w, u, v)
                heapq.heappush(pq, (dist[v], v))
    tree = [e for e in par if e]
    return sum(e[0] for e in tree), tree


print(dijkstra_tree(3, tri, 0))              # (5, ...): 같은 그래프, 다른 목표
```

같은 그래프에서 두 트리의 비용이 `4` 와 `5` 로 다르다. 두 최적화가 별개의 문제임이 수치로 드러난다.

## 복잡도와 다른 알고리즘

Kruskal 은 간선 정렬이 `O(|E| log|E|)` 이고 나머지가 서로소 집합 연산이라 거의 선형이므로 전체가 정렬에 지배된다. 가중치가 작은 정수여서 기수 정렬을 쓸 수 있으면 더 빨라진다.

| 알고리즘 | 복잡도 | 특징 |
|---|---|---|
| Kruskal | `O(\|E\| log\|E\|)` | 간선 정렬 + 서로소 집합 |
| Prim (이진 힙) | `O(\|E\| log\|V\|)` | 밀집 그래프에 유리 |
| Borůvka | `O(\|E\| log\|V\|)` | 병렬화하기 쉬움 |

세 알고리즘 모두 컷 성질의 서로 다른 적용이다. Borůvka 는 각 성분이 동시에 가장 가벼운 나가는 간선을 고르므로 분산 환경에 적합하고, Karger–Klein–Tarjan 의 무작위 선형 시간 알고리즘도 이 발상을 쓴다.

## matroid 로서의 성격

순환 없는 간선 집합들의 족은 그래프 matroid 의 독립집합족이고, 신장트리가 그 기저다. matroid 에서 탐욕법이 최적 기저를 준다는 일반 정리가 Kruskal 알고리즘의 정당성을 곧바로 함의한다.

이 관점의 이득은 일반화에 있다. 벡터의 일차독립성, 횡단 조건, 분할 제약이 모두 matroid 를 이루므로 같은 탐욕법이 통한다. 거꾸로 탐욕법이 실패하는 문제는 대개 matroid 구조가 없는 경우이며, 그 구분을 미리 할 수 있다는 것이 이론의 실용적 가치다.

# 활용

## 연결 비용의 최소화

통신망, 배전망, 도로망의 총 설치비를 줄이는 기본 모델이다. 다만 이 모델은 연결만 보장하므로 장애 대비 여분 경로, 최대 이동거리 제한, 용량 제약은 별도로 넣어야 한다. 실제 설계에서는 최소 신장트리를 출발점으로 삼고 제약을 추가해 나가는 방식이 흔하다.

## 군집화와 근사

간선을 가벼운 순서로 합치다가 `k` 개 성분이 남았을 때 멈추면 단일 연결 군집화가 된다. 최소 신장트리에서 가장 무거운 간선 `k-1` 개를 지운 것과 같으며, 트리 하나만 계산해 두면 모든 `k` 에 대한 군집화를 얻는다.

외판원 문제의 2-근사 알고리즘도 최소 신장트리를 쓴다. 트리를 두 번 순회하면 최적 경로 길이의 두 배를 넘지 않으며, Christofides 알고리즘은 여기에 최소 가중치 완전 매칭을 더해 `1.5` 로 개선한다. 다루기 쉬운 하한을 주는 구조라는 점이 [근사 알고리즘](approximation-algorithms.md)에서 반복해 쓰이는 이유다.[^1]

[^1]: Robert Sedgewick and Kevin Wayne, *Algorithms*, §4.3 Minimum Spanning Trees. 컷 성질, Kruskal 과 Prim 알고리즘, 시간 복잡도. https://algs4.cs.princeton.edu/43mst/

# 연관 문서

## 선수지식

- [Matroid](matroids.md)
- [서로소 집합 자료구조](union-find.md)

## 더 알아보기

아직 연결한 문서가 없다.

#graph_theory #algorithms
