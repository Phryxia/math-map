# 최소 신장트리

최소 신장트리는 모든 정점을 연결하는 트리 중 간선 가중치의 합이 가장 작은 것이다.

## 정의

G=(V,E)는 정점이 하나 이상인 유한 연결 무방향 그래프이고, w(e)는 간선 e의 실수 가중치라 하자. 신장트리 T는 모든 정점을 포함하고 연결되며 순환이 없는 간선 부분집합이다. 최소 신장트리는 다음 합을 최소화한다.

$$
w(T)=\sum_{e\in T}w(e)
$$

가중치에 음수와 동률을 허용한다. 그래프가 연결되지 않으면 모든 정점의 신장트리는 없고, 각 연결 성분의 최소 신장트리를 모은 최소 신장숲을 구한다.

## 성질

Kruskal 알고리즘은 간선을 가벼운 순서로 훑어, 이미 선택한 간선과 순환을 만들지 않을 때만 추가한다. 연결 그래프에서는 간선 |V|-1개를 선택하면 끝난다. 이는 그래프 matroid의 최소 가중치 기저 문제다.

정당성은 교환 논증으로 보인다. 현재 선택한 숲 F를 포함하는 최소 신장트리 T가 있다고 하자. 다음 간선 e가 T에 없으면 T에 e를 넣어 유일한 순환을 만든다. 이 순환에는 e의 한쪽 F-성분을 벗어나는 다른 간선 f가 있다. f는 F에 없고 선택 가능한 후보였으므로 w(e)≤w(f)다. T에서 f를 빼고 e를 넣어도 최소성은 유지된다. 처음 F가 비어 있을 때부터 이 논증을 반복한다.

가중치가 모두 다르면 최소 신장트리는 유일하다. 동률이 있으면 여러 개일 수 있지만 동률 자체가 여러 해를 보장하지는 않는다.

예를 들어 삼각형의 가중치가 AB=2, BC=2, AC=3이면 최소 신장트리는 AB와 BC로 총 4다. A에서 각 정점까지의 최단 경로를 동시에 보존하려면 AB와 AC가 필요하므로 총 5다. 두 최적화 목표는 다르다.

## 활용

모든 거점을 연결하는 통신선·도로의 총 설치비를 줄이는 기본 모델이다. 장애 대비 여분 경로나 최대 이동거리 조건은 별도로 모델링해야 한다. 간선 정렬과 효율적인 성분 관리로 Kruskal 알고리즘을 O(|E| log |E|) 시간에 구현할 수 있다.

## 기타 참고 문헌

- [Robert Sedgewick·Kevin Wayne, Algorithms §4.3](https://algs4.cs.princeton.edu/43mst/): 가중치 가정, cut property, Kruskal 알고리즘과 시간 복잡도.

# 연관 문서

## 선수지식

- [Matroid](matroids.md)
- [서로소 집합 자료구조](union-find.md)

## 더 알아보기

아직 연결한 문서가 없다.
