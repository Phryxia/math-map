# 논문: Graph Sparsification by Effective Resistances

# 개요

간선이 많은 그래프에서 Laplacian 계산은 간선 수에 비례해 비싸진다. 그래프를 간선이 적은 다른 그래프로 바꾸되 계산 결과가 거의 같기를 바라는 것이 희소화다. 문제는 "거의 같다" 를 무엇으로 정의하느냐다.

Spielman 과 Srivastava 는 가장 강한 기준을 택한다. 모든 정점값 배정에 대해 Laplacian 이차형식이 $1 \pm \epsilon$ 배 안에 들어야 한다. 특정 질의 하나가 아니라 무한히 많은 질의를 동시에 보존하는 요구다. 그런데도 정점 수의 로그배 개수의 간선이면 충분하다는 것이 이 논문의 결과다.

방법은 놀랄 만큼 단순하다. 각 간선을 [유효저항](effective-resistance.md)에 비례하는 확률로 뽑아 가중치를 보정한다. 우회로가 많은 간선은 저항이 낮으므로 버려도 되고, 대체 불가능한 간선은 저항이 높으므로 거의 확실히 살아남는다. 무작위 선택이 구조적 중요도를 그대로 반영한다[^1].

# 직관

## 왜 유효저항이 올바른 중요도인가

간선의 가중치가 크다고 중요한 것이 아니다. 두꺼운 간선이라도 옆에 비슷한 경로가 백 개 있으면 지워도 전체 전도는 거의 변하지 않는다. 반대로 가는 간선이라도 다리라면 지우는 순간 그래프가 끊어진다.

유효저항이 정확히 이 차이를 잰다. 곱 $w_eR_e$ 는 $[0,1]$ 에 있고, 1 에 가까우면 다리에 가깝고 0 에 가까우면 잉여다. 게다가 이 값은 무작위 신장트리가 그 간선을 포함할 확률과 같다. 즉 샘플링 확률이 "이 간선이 뼈대에 속할 확률" 이라는 조합적 의미를 가진다.

## 왜 정점 수만큼이면 되는가

Foster 정리가 답을 준다.

$$
\sum_{e\in E}w_eR_e=n-1
$$

중요도의 총합이 간선 수와 무관하게 $n-1$ 로 고정되어 있다. 간선이 아무리 많아도 중요도는 정점 수만큼만 나눠 가진다는 뜻이다. 그러니 중요도에 비례해 뽑으면 필요한 표본 수가 $n$ 에 로그 인자만 붙어 결정된다.

```mermaid
graph LR
  A["간선 m 개<br/>가중치가 제각각"] --> B["중요도 w_e R_e<br/>총합은 항상 n-1"]
  B --> C["중요도에 비례해<br/>q ≈ n log n / ε² 번 복원 추출"]
  C --> D["간선 O(n log n / ε²) 개<br/>모든 이차형식을 1±ε 로 근사"]
```

## 무한히 많은 조건을 한꺼번에

모든 $x$ 에 대한 부등식은 조건이 무한히 많다. 각각에 집중부등식을 쓰고 합집합 상계를 취하는 방식은 통하지 않는다.

논문은 문제를 행렬 하나에 대한 진술로 바꾼다. 샘플링을 적절히 정규화하면 목표가 "무작위 행렬의 합이 항등행렬에 가깝다" 는 형태가 되고, 행렬의 스펙트럼 노름에 대한 집중부등식 한 번으로 모든 $x$ 가 동시에 처리된다. 벡터별 논증을 행렬 논증으로 올리는 것이 증명의 요체다.

# 정의

## Spectral sparsifier

$G$ 를 정점 $n$ 개의 연결 가중 무향 그래프, $L_G$ 를 그 Laplacian 이라 한다. $H$ 는 같은 정점 집합 위에서 $G$ 의 간선 일부에 새 가중치를 준 그래프이고, 오차 허용량은 $0 < \epsilon < 1$ 이다.

$H$ 가 $G$ 의 spectral sparsifier 라는 것은 모든 실수 벡터 $x$ 에 대해 다음이 성립한다는 뜻이다.

$$
(1-\varepsilon)\,x^{\mathsf T}L_Gx\ \le\ x^{\mathsf T}L_Hx\ \le\ (1+\varepsilon)\,x^{\mathsf T}L_Gx
$$

특정 신호 하나가 아니라 모든 정점값 배정에 같은 부등식이 적용된다.

## 샘플링 분포

간선 $e$ 의 전도도를 $w_e$, 양 끝 사이 유효저항을 $R_e$ 라 하고 다음 확률을 쓴다.

$$
p_e=\frac{w_eR_e}{n-1}
$$

Foster 정리에 의해 연결 그래프에서 $\sum_e p_e = 1$ 이므로 이것은 확률분포다. $q$ 번 독립적으로 복원 추출하고, 뽑힌 간선의 가중치에 매번 다음을 더한다.

$$
\frac{w_e}{q\,p_e}
$$

뽑히지 않은 간선은 $H$ 에 없다. 한 간선이 여러 번 뽑히면 가중치가 누적된다.

## 계산 절차

```python
import random

def sparsify(edges, resistance, n, q):
    """edges: [(u, v, w)], resistance: {(u,v): R_e}. q 번 복원 추출."""
    p = [w * resistance[(u, v)] / (n - 1) for u, v, w in edges]
    out = {}
    for _ in range(q):
        i = random.choices(range(len(edges)), weights=p)[0]
        u, v, w = edges[i]
        out[(u, v)] = out.get((u, v), 0.0) + w / (q * p[i])
    return [(u, v, w) for (u, v), w in out.items()]
```

실제 구현에서 `resistance` 는 정확한 값이 아니라 아래에서 말하는 근사값을 쓴다.

# 성질

## 기대값 일치

간선 $e$ 가 $H$ 에서 받는 가중치의 기대값은 $q \cdot p_e \cdot w_e/(q p_e) = w_e$ 다. 따라서 $\mathbb{E}[L_H] = L_G$ 다.

기대값이 맞는 것은 쉽고, 논문의 내용이 아니다. 핵심은 편차가 모든 이차형식에서 동시에 작다는 것이다.

## 주 정리

읽은 arXiv 판의 Theorem 1 은 다음을 말한다. 충분히 큰 $n$ 과 충분히 큰 절대상수 $C$ 에 대해

$$
\frac1{\sqrt n}<\varepsilon<1,\qquad q=\left\lceil\frac{Cn\log n}{\varepsilon^2}\right\rceil
$$

이면 위 절차로 얻은 $H$ 가 $G$ 의 $\epsilon$ spectral sparsifier 일 확률이 적어도 $1/2$ 다.

이것은 이 판본의 정리 조건이다. 더 작은 오차 범위나 더 높은 성공 확률에 대한 변형을 같은 문장에 섞지 않는다. 성공 확률은 독립 반복과 검증으로 올릴 수 있다.

간선 수는 $q$ 이하이므로 $O(n \log n / \epsilon^2)$ 이며, 원래 간선 수 $m$ 과 무관하다. 완전그래프에서는 $m = \Theta(n^2)$ 이 $O(n \log n)$ 으로 줄어든다.

## 증명의 뼈대

간선 $e=(u,v)$ 에 대해 $b_e=e_u-e_v$ 로 두면 $L_G = \sum_e w_e b_e b_e^{\mathsf T}$ 다. 여기에 $L_G^{+/2}$ 를 양쪽에서 곱해 정규화한다.

$$
\Pi=L_G^{+/2}L_GL_G^{+/2}=\sum_e\big(\sqrt{w_e}\,L_G^{+/2}b_e\big)\big(\sqrt{w_e}\,L_G^{+/2}b_e\big)^{\mathsf T}
$$

$\Pi$ 는 $L_G$ 의 상에 대한 사영이다. 이때 각 항의 노름 제곱이 정확히 $w_e R_e$ 이므로, 샘플링 확률 $p_e$ 는 각 항의 기여도에 비례하는 분포가 된다.

목표는 $\|L_G^{+/2}L_HL_G^{+/2} - \Pi\| \le \epsilon$ 이다. 이 노름 부등식은 모든 $x$ 에 대한 이차형식 부등식과 동치이므로, 무한히 많던 조건이 하나로 줄어든다. 남은 일은 독립 무작위 행렬 $q$ 개의 평균이 기대값에 집중한다는 것을 보이는 것이고, 논문은 Rudelson 의 무작위 벡터 합에 대한 결과를 쓴다.

## cut 은 포함된다

정점 집합을 $S$ 와 그 여집합으로 나누는 cut 에서, $x$ 를 $S$ 의 지시벡터로 잡으면 $x^{\mathsf T}L_Gx$ 가 정확히 cut 을 가로지르는 가중치의 합이다. 따라서 spectral 근사는 모든 cut 을 동시에 $1\pm\epsilon$ 로 근사한다.

역은 성립하지 않는다. 모든 cut 을 보존하는 cut sparsifier 가 모든 이차형식을 보존하지는 않는다. spectral 이 더 강한 요구다.

## 보존하지 않는 것

최단 경로 길이가 같은 비율로 보존된다는 정리가 아니다. 간선이 통째로 사라질 수 있으므로 두 정점 사이 거리가 크게 늘어날 수 있다. 거리를 보존하는 희소화는 spanner 라는 다른 대상이고 다른 보장을 가진다. 무엇을 보존하도록 설계한 근사인지 늘 구분해야 한다.

## 유효저항을 어떻게 구하는가

모든 간선의 정확한 유효저항은 $L^+$ 를 요구하므로 희소화보다 비싸다. 논문 Section 4 가 이 문제를 푼다. Johnson–Lindenstrauss 사영으로 $O(\log n)$ 차원에 모든 정점을 매장하면 모든 유효저항을 상수배 안에서 동시에 근사할 수 있고, 각 사영 벡터는 Laplacian 선형계를 한 번 푸는 것으로 얻는다. 빠른 Laplacian 해법과 결합하면 근사 전체가 준선형 시간이다.

상수배 근사로도 충분하다. 샘플링 확률이 상수배 안에서 맞으면 표본 수만 상수배 늘어난다.

# 활용

## Laplacian 계산의 전처리

Laplacian 선형계, 고유값 근사, 확산 계산에서 먼저 희소화한 뒤 계산하면 간선 수가 $O(n \log n)$ 으로 줄어든다. 결과의 오차는 $\epsilon$ 로 통제된다. 반복적 Laplacian 해법의 성능이 간선 수에 직접 의존하므로 효과가 크다.

## 무작위 알고리즘의 표본 설계

이 논문의 구조는 그래프 밖에서도 되풀이된다. 항들의 합으로 쓰인 행렬에서 각 항의 "레버리지 점수" 에 비례해 뽑으면 합 전체를 근사할 수 있다는 것이 일반적 형태이고, 유효저항은 그래프에서의 레버리지 점수다. 무작위 수치선형대수의 열 샘플링이 같은 원리를 쓴다.

## 이후의 발전

$O(n \log n / \epsilon^2)$ 은 최적이 아니다. Batson, Spielman, Srivastava 의 후속 연구는 $O(n/\epsilon^2)$ 개의 간선으로 충분함을 보였다. 다만 그 구성은 무작위 샘플링이 아니라 결정론적이고 훨씬 느리다. 단순함과 표본 크기의 교환이 이 계열 연구의 축이다.

[^1]: Spielman, Srivastava, *Graph Sparsification by Effective Resistances*, arXiv 2008 / SIAM J. Comput. 2011, https://arxiv.org/abs/0803.0929. Theorem 1 의 조건, Section 3 의 행렬 집중 논증, Section 4 의 유효저항 근사 계산. 기대 가중치 확인은 본문에서 직접 계산했다.

# 연관 문서

## 선수지식

- [유효저항](effective-resistance.md)
- [유한 확률 공간](probability.md)

## 더 알아보기

아직 연결한 문서가 없다.

#graph_theory #algorithms #paper
