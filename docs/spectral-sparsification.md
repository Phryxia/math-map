# 스펙트럼 희소화

# 개요

간선이 많은 [그래프](graphs.md)에서 Laplacian 계산은 간선 수에 비례해 비싸진다. 그래프를 간선이 적은 다른 그래프로 바꾸되 계산 결과가 거의 같게 만드는 것이 희소화이고, 남는 문제는 "거의 같다" 의 기준이다.

Spielman 과 Srivastava 는 모든 정점값 배정에 대해 Laplacian 이차형식이 $1 \pm \epsilon$ 배 안에 들어야 한다는 기준을 택한다. 질의 하나가 아니라 무한히 많은 질의를 동시에 보존하는 요구인데도 정점 수의 로그배 개수의 간선이면 충분하다는 것이 이 논문의 결과다.

방법은 각 간선을 [유효저항](effective-resistance.md)에 비례하는 확률로 뽑아 가중치를 보정하는 것이다. 우회로가 많은 간선은 저항이 낮아 버려도 되고, 대체 불가능한 간선은 저항이 높아 거의 확실히 남는다[^1].

# 직관

## 유효저항의 의미

가중치가 큰 간선이라도 옆에 비슷한 경로가 백 개 있으면 지워도 전체 전도가 거의 변하지 않고, 가는 간선이라도 다리면 지우는 순간 그래프가 끊어진다.

곱 $w_eR_e$ 는 $[0,1]$ 에 있고 1 에 가까우면 다리에 가깝고 0 에 가까우면 잉여다. 이 값은 무작위 신장트리가 그 간선을 포함할 확률과 같으므로, 샘플링 확률이 간선이 신장트리에 속할 확률이라는 조합적 의미를 갖는다.

## Foster 정리와 표본 수

$$
\sum_{e\in E}w_eR_e=n-1
$$

중요도의 총합이 간선 수와 무관하게 $n-1$ 로 고정된다. 간선이 아무리 많아도 중요도는 정점 수만큼만 나뉘므로, 중요도에 비례해 뽑으면 필요한 표본 수가 $n$ 에 로그 인자만 붙어 결정된다.

## 행렬 논증

모든 $x$ 에 대한 부등식은 조건이 무한히 많아 각각에 집중부등식을 쓰고 합집합 상계를 취하는 방식이 통하지 않는다.

논문은 문제를 행렬 하나에 대한 진술로 바꾼다. 샘플링을 정규화하면 목표가 무작위 행렬의 합이 항등행렬에 가깝다는 형태가 되고, 스펙트럼 노름에 대한 집중부등식 한 번으로 모든 $x$ 가 처리된다.

# 정의

## Spectral sparsifier

$G$ 를 정점 $n$ 개의 연결 가중 무향 그래프, $L_G$ 를 그 Laplacian 이라 한다. $H$ 는 같은 정점 집합 위에서 $G$ 의 간선 일부에 새 가중치를 준 그래프이고, 오차 허용량은 $0 \lt\epsilon \lt 1$ 이다.

$H$ 가 $G$ 의 spectral sparsifier 라는 것은 모든 실수 벡터 $x$ 에 대해

$$
(1-\varepsilon)\thinspace x^{\mathsf T}L_Gx\ \le\ x^{\mathsf T}L_Hx\ \le\ (1+\varepsilon)\thinspace x^{\mathsf T}L_Gx
$$

가 성립한다는 뜻이다.

## 샘플링 분포

간선 $e$ 의 전도도를 $w_e$ , 양 끝 사이 유효저항을 $R_e$ 라 하고

$$
p_e=\frac{w_eR_e}{n-1}
$$

를 쓴다. Foster 정리에 의해 연결 그래프에서 $\sum_e p_e = 1$ 이므로 확률분포다. $q$ 번 독립적으로 복원 추출하고 뽑힌 간선의 가중치에 매번 $w_e/(q\thinspace p_e)$ 를 더한다. 뽑히지 않은 간선은 $H$ 에 없고, 한 간선이 여러 번 뽑히면 가중치가 누적된다.

## 계산 절차

각 간선을 유효저항에 비례하는 확률로 뽑고 뽑힌 확률의 역수로 가중치를 키운다. 실제로는 유효저항의 정확한 값이 아니라 아래의 근사값을 쓴다.

# 성질

## 기대값 일치

간선 $e$ 가 $H$ 에서 받는 가중치의 기대값은 $q \cdot p_e \cdot w_e/(q p_e) = w_e$ 이므로 $\mathbb{E}[L_H] = L_G$ 다. 논문의 내용은 편차가 모든 이차형식에서 동시에 작다는 것이다.

## 주 정리

arXiv 판 Theorem 1 은 충분히 큰 $n$ 과 충분히 큰 절대상수 $C$ 에 대해

$$
\frac1{\sqrt n}\lt\varepsilon\lt 1,\qquad q=\left\lceil\frac{Cn\log n}{\varepsilon^2}\right\rceil
$$

이면 위 절차로 얻은 $H$ 가 $G$ 의 $\epsilon$ spectral sparsifier 일 확률이 적어도 $1/2$ 라고 말한다. 성공 확률은 독립 반복과 검증으로 올린다.

간선 수는 $q$ 이하이므로 $O(n \log n / \epsilon^2)$ 이고 원래 간선 수 $m$ 과 무관하다. 완전그래프에서는 $m = \Theta(n^2)$ 이 $O(n \log n)$ 으로 줄어든다.

## 증명의 요지

간선 $e=(u,v)$ 에 대해 $b_e=e_u-e_v$ 로 두면 $L_G = \sum_e w_e b_e b_e^{\mathsf T}$ 다. 여기에 $L_G^{+/2}$ 를 양쪽에서 곱해 정규화한다.

$$
\Pi=L_G^{+/2}L_GL_G^{+/2}=\sum_e\big(\sqrt{w_e}\thinspace L_G^{+/2}b_e\big)\big(\sqrt{w_e}\thinspace L_G^{+/2}b_e\big)^{\mathsf T}
$$

$\Pi$ 는 $L_G$ 의 상에 대한 사영이고, 각 항의 노름 제곱이 정확히 $w_e R_e$ 이므로 샘플링 확률 $p_e$ 가 각 항의 기여도에 비례한다.

목표는 $\Vert L_G^{+/2}L_HL_G^{+/2} - \Pi\Vert \le \epsilon$ 이다. 이 노름 부등식이 모든 $x$ 에 대한 이차형식 부등식과 동치이므로 무한히 많던 조건이 하나로 줄고, 남은 일은 독립 무작위 행렬 $q$ 개의 평균이 기대값에 집중한다는 것이다. 논문은 Rudelson 의 무작위 벡터 합에 대한 결과를 쓴다.

## cut 근사

$x$ 를 $S$ 의 지시벡터로 잡으면 $x^{\mathsf T}L_Gx$ 가 cut 을 가로지르는 가중치의 합이므로, spectral 근사는 모든 cut 을 동시에 $1\pm\epsilon$ 로 근사한다.

역은 성립하지 않는다. 모든 cut 을 보존하는 cut sparsifier 가 모든 이차형식을 보존하지는 않으며, spectral 이 더 강한 요구다.

## 보존되지 않는 양

간선이 통째로 사라질 수 있으므로 두 정점 사이 거리는 크게 늘어날 수 있고, 최단 경로 길이는 보존되지 않는다. 거리를 보존하는 희소화는 spanner 라는 다른 대상이며 다른 보장을 갖는다.

## 유효저항의 근사 계산

모든 간선의 정확한 유효저항은 $L^+$ 를 요구하므로 희소화보다 비싸다. 논문 Section 4 는 Johnson–Lindenstrauss 사영으로 $O(\log n)$ 차원에 모든 정점을 매장해 유효저항을 상수배 안에서 동시에 근사하고, 각 사영 벡터는 Laplacian 선형계를 한 번 푸는 것으로 얻는다. 빠른 Laplacian 해법과 결합하면 근사 전체가 준선형 시간이다.

샘플링 확률이 상수배 안에서 맞으면 표본 수만 상수배 늘어나므로 상수배 근사로 충분하다.

# 활용

- **Laplacian 계산의 [전처리](preconditioning.md).** Laplacian 선형계, 고유값 근사, 확산 계산에서 먼저 희소화하면 간선 수가 $O(n \log n)$ 으로 줄고 오차는 $\epsilon$ 로 통제된다. 반복적 Laplacian 해법의 성능이 간선 수에 직접 의존한다.
- **무작위 알고리즘의 표본 설계.** 항들의 합으로 쓰인 행렬에서 각 항의 레버리지 점수에 비례해 뽑으면 합 전체를 근사할 수 있고, 유효저항은 그래프에서의 레버리지 점수다. 무작위 수치선형대수의 열 샘플링이 같은 원리를 쓴다.
- **이후의 발전.** Batson, Spielman, Srivastava 는 $O(n/\epsilon^2)$ 개의 간선으로 충분함을 보였다. 그 구성은 무작위 샘플링이 아니라 결정론적이고 훨씬 느리며, 단순함과 표본 크기의 교환이 이 계열 연구의 축이다.

[^1]: Spielman, Srivastava, *Graph Sparsification by Effective Resistances*, arXiv 2008 / SIAM J. Comput. 2011, https://arxiv.org/abs/0803.0929. Theorem 1 의 조건, Section 3 의 행렬 집중 논증, Section 4 의 유효저항 근사 계산.

# 연관 문서

## 선수지식

- [유효저항](effective-resistance.md)
- [유한 확률 공간](probability.md)

## 더 알아보기

아직 연결한 문서가 없다.

#graph_theory #algorithms #paper
