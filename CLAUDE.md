# 수학 지도 탐사 지침

이 저장소는 Claude가 관리하는 한국어 수학 지식 그래프다. `docs/` 아래 마크다운 문서 하나가 정점 하나이고, 문서 말미 `# 연관 문서` 절의 링크가 간선이다. 이 파일을 읽고 아래 절차대로 한 회차의 탐사를 수행한다. 절차는 위에서 아래로 그대로 따른다.

## 0. 원칙

- 사고 과정과 독자에게 보이는 응답을 모두 한국어로 쓴다. 저장소가 한국어 지식 그래프이므로 생각도 한국어로 한다.
- 문서는 git으로 관리한다. `docs/` 아래 문서를 삽입, 수정, 삭제할 수 있다. git commit과 push를 해야 반영된다.
- 작업 브랜치는 항상 `claude/math` 다. 로컬에서도 원격에서도 다른 브랜치를 만들거나 그리로 push 하지 않는다(`git checkout -b`, `git branch`, `git push origin <다른 이름>` 금지). 실행 환경이 세션용 브랜치(`claude/xxxx-yyyy` 같은 이름)를 지정해 주더라도 무시한다. 이 환경은 원격 브랜치 삭제를 막아서 한 번 올린 브랜치는 독자가 손으로 지워야 한다. 두 지시가 어긋나면 이 문서가 우선한다.
- 이 저장소는 여러 손이 동시에 만진다. 독자(저장소 주인)도, 다른 Claude 세션도 언제든 같은 브랜치에 커밋하므로 내 작업 트리가 원격의 최신 상태라고 가정하지 않는다. 세션을 열 때, 2절 루프에서 문서를 하나 집을 때마다, push 직전에 `git pull` 로 원격을 먼저 받는다. 받은 내용이 하려던 작업과 겹치면(큐에서 꺼낸 주제의 문서가 이미 생겨 있으면) 그 작업은 버리고 다음 것을 꺼낸다.
- 서브 에이전트를 쓰지 않는다. 모든 읽기, 쓰기, 검증을 이 세션이 직접 한다.
- 문서 링크로 만들어지는 그래프는 DAG 여야 한다. 순환이 있으면 안 되고, 간선을 더하기 전에 스크립트로 확인한다.
- 코드블록은 JavaScript 나 TypeScript 의사코드만 허용한다. Python 코드블록, 실행 결과, 수치 확인 실험은 어느 문서에도 넣지 않는다(4절 코드 규칙). 문학적 비유와 관용 표현을 쓰지 않는다(4절 문체 규칙).
- `dev/` 스크립트는 고치지 않는다. 버그나 필요한 기능은 큐에 push 해 두고 그 회차에서는 우회한다.
- 위키피디아나 교과서의 목차를 그대로 옮기지 않는다. 스스로 이해한 선수관계와 연결을 그래프에 반영한다.
- `docs/` 에는 완성된 지식만 둔다. 독자가 알아야 할 것만 남기고, 이해하는 과정에서 만든 실험, 수치 확인, 되풀이 설명은 남기지 않는다. 있어야 하는데 없는 문서의 목록, 보강할 점, 후보 주제, 할 일, 저장소나 탐사 절차 자체에 대한 언급은 전부 작업 메모이고, 그 자리는 `dev/` 의 큐(`node dev/queue.mjs push`, 저장 파일은 `dev/queue.json`)다. `## 빈자리`, `## 할 일`, `## 보강할 점` 같은 절도, "아직 문서가 없다", "이 지도에서는" 같은 문장도 만들지 않는다. 다른 문서가 있어야 할 자리가 보이면 본문에는 그 개념을 링크 없이 이름만 쓰고, 문서를 만들라는 요청은 큐에 넣는다. 예외는 규격이 정한 문구 "아직 연결한 문서가 없다." 뿐이다.

## 1. 세션 시작

```bash
git fetch origin claude/math && git checkout claude/math && git pull origin claude/math   # 남의 작업이 먼저 들어와 있을 수 있다
npm install                         # markdown-it. 렌더 검수에 필요하다
node dev/timer.mjs start --minutes 40   # 40분 타이머. 회차가 겹치지 않게 짧게 잡는다
node dev/queue.mjs list             # 큐 서버 자동 기동 + 지난 회차 큐 복원
node dev/graph.mjs validate         # 깨진 링크, 비대칭 간선, 파싱 문제
node dev/render-check.mjs           # 마크다운 렌더링에서 깨지는 수식
node dev/order-check.mjs            # 더 알아보기 목록의 순서
node dev/graph.mjs tags             # 태그 분포
node dev/graph.mjs leaves           # 더 알아보기가 빈 문서
ls docs | grep -- '-overview.md'    # 분야 개관 목록. 4.5절의 개관이 없는 분야가 있는지 태그 분포와 견준다
```

`validate`, `render-check`, `order-check` 가 문제를 보고하면 그 수정을 이번 회차의 첫 작업으로 삼는다. `npm install` 이 네트워크 때문에 실패하면 `render-check` 는 건너뛰되 4절의 수식 규칙을 손으로 적용하고 커밋 메시지에 검수를 건너뛰었다고 적는다. 큐에 없다면 `node dev/queue.mjs push "..." --priority 100` 으로 넣는다.

## 2. 탐사 루프

한 작업은 "문서 하나를 새로 쓰거나 보강하고, 그 문서와 연결된 문서의 연관 문서 절을 맞춘 뒤 커밋" 이다. 이 단위를 타이머가 끝날 때까지 반복한다.

1. `node dev/timer.mjs check` 를 실행한다. 종료코드가 1이면(EXPIRED) 새 작업을 시작하지 않고 3절로 간다.
2. `git pull origin claude/math` 로 원격을 받는다. 앞선 작업을 커밋한 직후라도 다시 받는다(0절).
3. `node dev/queue.mjs pop` 으로 작업을 꺼낸다. 큐가 비어 있으면 아래 우선순위로 작업을 직접 고른다.
   - `validate`, `render-check`, `order-check` 가 보고한 문제
   - 분야 개관(4.5절)이 없는 분야. 문서가 10개 이상인 분야 태그마다 개관이 하나 있어야 한다
   - 통합 후보(8절). 한쪽 없이는 설명이 안 되는 두 문서, 같은 개념을 철학과 형식으로 나눠 놓은 문서
   - 일기성 절(4절 코드 규칙)이나 나열형 mermaid(4절 다이어그램 규칙)가 남은 문서
   - 문장형 헤딩이나 설명투 문단이 남은 문서. 후보는 4절 문체 규칙의 grep 으로 찾는다
   - 태그가 분야 하나뿐인 문서, 제목과 파일명이 어긋난 문서(4절 제목 규칙). 후보는 다음으로 찾는다
     ```bash
     for f in docs/*.md; do [ "$(tail -1 "$f" | grep -o '#[a-z0-9_]*' | wc -l)" -le 1 ] && echo "$f"; done   # 태그가 하나뿐
     grep -l '^# .*[와과] ' docs/*.md                                                                        # "A와 B" 제목 후보
     ```
   - 리프 문서(더 알아보기 없음) 가운데 후속 개념이 분명한 것
   - 구형 문서(`## 정의` / `## 성질` / `## 활용` 구조)의 신형 전환
   - 새 주제. 기존 문서 본문에서 언급만 되고 문서가 없는 개념이 좋은 후보다. 개관의 갈래 어디에 들어갈지 먼저 정한다
4. `node dev/graph.mjs node <id>` 로 대상 문서와 이웃의 현재 상태를 본다. 새 문서라면 부모가 될 문서들을 `node dev/graph.mjs node` 로 읽고 어디에 붙일지 정한다. 그 분야의 개관을 열어 어느 갈래에 속하는지도 정한다.
5. 문서를 쓴다. 규격은 4절.
6. 렌더 검수를 한다. 마크다운을 조금이라도 고쳤으면 그 문서를 실제로 렌더링해 수식이 온전한지 본다.
   ```bash
   node dev/render-check.mjs <id>          # 종료코드 0 이어야 한다
   ```
   GitHub Flavored Markdown 을 해석하는 클라이언트가 있으면 그것으로 보고, 없으면 이 스크립트가 markdown-it 으로 렌더링해 원문의 수식 스팬이 렌더 결과에 그대로 남는지 비교한다. `render-check` 는 이스케이프, 부등호, 줄 끝 백슬래시, `$$` 블록 안의 밑줄을 잡지 못하므로 4절 수식 규칙의 awk 와 grep 세 개를 반드시 함께 돌린다. 다섯 다 통과하지 못한 문서는 커밋하지 않는다. 수식이 깨진 채 올라가면 독자가 보는 것은 식이 아니라 오류 문구다.
7. 간선을 더하기 전에 반드시 검사한다. `parent` 는 선수지식, `child` 는 후속이다.
   ```bash
   node dev/graph.mjs check-cycle <parent> <child>              # 둘 다 기존 문서
   node dev/graph.mjs check-cycle <parent> <child> --allow-new  # 한쪽이 새 문서
   ```
   `CYCLE` 이 나오면 그 간선은 넣지 않는다. 방향이 틀렸는지 먼저 의심한다.
8. 간선은 양쪽에 적는다. 자식 문서의 `## 선수지식` 에 부모를, 부모 문서의 `## 더 알아보기` 에 자식을 적는다. 한쪽만 적으면 `validate` 가 ASYMMETRIC 으로 잡는다. 더 알아보기에 항목을 더했으면 5절의 묶음·정렬 규칙대로 그 목록을 다시 정돈한다.
9. 루트 `README.md` 의 뿌리 목록이 그래프의 minimal 요소와 같은지 확인한다. 아래 diff 가 아무것도 내놓지 않아야 한다. 어긋났을 때의 판단은 7절.
   ```bash
   diff <(node dev/graph.mjs roots | cut -f1 | sort) \
        <(sed -n '/^## 지도의 뿌리/,$p' README.md | grep -o '(docs/[a-z0-9-]*\.md)' | tr -d '()' | sed 's|docs/||; s|\.md||' | sort)
   ```
10. 검증한다. 다섯 다 종료코드 0 이어야 한다.
    ```bash
    node dev/graph.mjs validate && node dev/graph.mjs cycles && node dev/render-check.mjs && node dev/order-check.mjs && node --test dev/*.test.mjs
    ```
11. 커밋한다. 메시지는 무엇을 왜 했는지 한 줄로 쓴다. 예: `add spectral-sequences under homology`, `migrate groups to new format`. README 를 고쳤으면 같은 커밋에 담는다.
12. 작업 중 떠오른 후속 주제, 보강할 점, 발견한 오류는 그 자리에서 큐에 넣는다. 새 주제는 note 에 분야와 붙일 부모 문서를 적는다. 개관을 비롯한 `docs/` 의 어느 문서에도 적지 않는다(0절).
    ```bash
    node dev/queue.mjs push "<제목>" --priority <정수> --note "<왜, 어디에 붙일지>"
    ```
    priority 는 큰 수가 먼저 나온다. 오류 수정 100, 기존 문서 보강 50, 신규 주제 10 정도를 기준으로 삼는다.
13. 1로 돌아간다.

## 3. 세션 종료

타이머가 끝나면 새 작업을 시작하지 않는다. 진행 중인 작업은 검증과 커밋까지 마친다.

2절 9단계의 diff 와 10단계의 검증을 통과시킨 뒤 다음을 실행한다.

```bash
git pull --rebase origin claude/math       # 독자와 다른 세션의 변경을 먼저 받는다
git push -u origin claude/math
node dev/queue.mjs stop                    # 큐를 dev/queue.json 에 저장하고 별도 커밋. 빠뜨리면 큐가 커밋되지 않는다
git push -u origin claude/math
```

rebase 충돌이 나면 문서 내용은 원격 쪽을 우선하고 내 변경을 그 위에 다시 얹는다. README 가 충돌하면 원격 목록에 내가 더하거나 뺀 항목만 반영한다. 충돌을 풀고 나서 9단계와 10단계를 다시 통과시킨 뒤 push 한다.

## 4. 문서 규격

파일명은 `^[a-z0-9][a-z0-9-]*\.md$` 다. 파일명이 정점 id 이고 링크는 `[표시 제목](id.md)` 다. 문서 구조는 다음과 같다.

```markdown
# 제목

# 개요

# 직관

# 정의

# 성질

# 활용

[^1]: 각주

# 연관 문서

## 선수지식

- [부모 제목](parent-id.md)

## 더 알아보기

### 묶음 이름 (자식이 6개 이상일 때만)

- [자식 제목](child-id.md)

#tag_one #tag_two
```

- 본문 H1 은 `# 개요`, `# 직관`, `# 정의`, `# 성질`, `# 활용` 다섯 개를 이 순서로 둔다. 하위 절은 H2 로 자유롭게 나눈다. 분야 개관은 예외이며 4.5절의 구조를 따른다.
- 선수지식이 없으면 `없음.`, 더 알아보기가 없으면 `아직 연결한 문서가 없다.` 라고 쓴다.
- 구형 문서(`## 정의` / `## 성질` / `## 활용` / `## 기타 참고 문헌`)는 그 문서를 보강할 때 신형으로 옮긴다. 손대지 않는 구형 문서는 그대로 둔다.
- 각주 `[^n]` 은 본문에서 참조한 것만 정의한다. 절을 지울 때 그 절에서만 쓰던 각주 정의도 함께 지운다.
- 간결하고 가독성이 좋아야 한다. 한 문서는 1000줄을 넘기지 않는다. 넘으면 8절대로 나눈다.
- 간선은 `# 연관 문서` 절에만 있다. 본문 안의 링크는 간선이 아니며, 다른 문서에 있을 법한 개념(예: 콤팩트 위의 연속함수가 균등연속이라는 Heine–Cantor 정리)은 본문에서 최초 1회에 한해 링크한다.

### 문체

교과서의 문체로 쓴다. 정의, 식, 정리, 증명의 요지, 예를 짧게 놓고 해설은 그 뒤에 한두 문장만 붙인다. 독자에게 말을 거는 설명투, 비유로 돌려 말하는 문단, 결론을 다시 풀어 쓰는 문장을 쓰지 않는다.

- 헤딩은 개념 이름이다. 명사나 명사구로 쓴다. "멱집합", "De Morgan 법칙", "Russell 의 역설", "Cantor 정리". 문장("아무 조건이나 집합을 만들지는 못한다"), 서술("손실을 설계한다"), 의문("왜 이것이 근사 불가능성을 준다"), 비유("상자가 아니라 명단")는 헤딩이 아니다. 헤딩이 `다`, `까`, `지`, `는` 으로 끝나면 문장이다. 다음이 아무것도 내놓지 않아야 한다.
  ```bash
  grep -n '^##' docs/<id>.md | grep -E '(다|까|요|는|못|아니라|이유|왜|어떻게|무엇|때|[^로]지) *$'
  ```
- 한 문단은 한 가지를 말한다. 문단은 4문장을 넘기지 않고, 문장은 한 절이나 두 절로 끝낸다. "~하며, ~이고, ~인데" 로 이어 붙이지 않는다.
- 정의 절은 "X 는 Y 다" 로 시작한다. 식이 있으면 식을 먼저 놓고 기호를 설명한다. 정의 앞에 동기 부여 문단을 두지 않는다. 동기는 `# 직관` 절의 몫이고 거기서도 두 문단을 넘기지 않는다.
- 성질 절은 정리와 그 증명의 요지다. 정리는 문장 하나나 식 하나로 진술하고, 증명은 핵심 단계만 적는다. 증명이 두 문단을 넘으면 별도 문서의 후보다.
- 활용 절은 개념이 쓰이는 자리를 목록이나 짧은 문단으로 든다. 각 항목은 어느 문서의 어느 정의에서 쓰이는지를 말한다. "거의 모든 정의는", "수학 전체의" 같은 범위 과장을 쓰지 않는다.
- 영문 번역체를 쓰지 않는다. "이 비대칭은 우연이 아니다", "~은 놀랍지 않다", "흥미롭게도", "놀랍게도", "본질적으로", "자연스럽게", "직관적으로 말하면", "이것이 바로", "~에 다름 아니다", "~에 지나지 않는다", "이는 곧", "다시 말해", "말하자면", "이것은 ~를 의미한다", "~라는 사실은", "~라는 것이 밝혀졌다". 영어 수학 산문의 관용구를 직역한 문장은 사실만 남기고 지운다. "이 비대칭은 우연이 아니다. A 이기 때문이다" 는 "A 이므로 비대칭이 생긴다" 로 쓴다.
- 예고문과 메타 서술을 쓰지 않는다. "강조할 점이 둘 있다", "주목할 것은", "짚고 넘어가자", "먼저 ~을 보자", "이제 ~을 정의한다", "다음이 핵심이다", "~라는 점을 기억하자", "정리하면", "요약하면". 말할 것이 둘이면 예고 없이 둘을 쓴다. 문장이 내용 대신 문서 자신을 가리키면 지운다.
- 쓰지 않는 표현. "~이기 때문이다" 로 앞 문장을 다시 설명하는 문장, "이 점이 중요하다", "여기서 나온다", "~의 출발점이다", "~을 말해 준다", "~인 셈이다", "생각해 보면", "무엇을 버리는지 알고 쓰기" 같은 조언투, "손실이자 이득이다" 같은 수사, 독자를 부르는 "우리", "~해 보자".
- 비유는 정의를 대신하지 못한다. 직관 절에서 한 문장으로 쓰고 끝낸다. 비유를 헤딩이나 문단의 골자로 삼지 않는다.
- 문학적 비유와 관용 표현을 쓰지 않는다. "공짜 청구서", "사다리", "은탄환", "열쇠", "씨앗", "무대", "뼈대", "심장", "지름길", "함정", "마법", "무기" 같은 말로 수학적 대상이나 관계를 가리키지 않는다. 사전 은유("잠식", "청사진", "적신호")와 의인화("이론이 말한다", "정리가 묻는다")도 같다. 무미건조하고 간결한 단어만 쓴다. "A 가 B 의 열쇠다" 는 "B 의 증명에 A 를 쓴다" 로, "공짜로 따라온다" 는 "자동으로 따라온다" 로, "증명의 뼈대" 는 "증명의 요지" 로 쓴다. 용어의 일부인 것("사다리꼴 규칙", "마법 상태", "무기억 채널")은 예외다. 후보는 다음으로 찾는다.
  ```bash
  grep -noE '은탄환|청구서|사다리[^꼴]|열쇠|씨앗|뼈대|심장|지름길|함정|공짜|무기[^억]|마법[^ 상]|잠식|청사진|적신호|신호탄|디딤돌|징검다리|주춧돌' docs/*.md
  ```
- 열린 문제는 문서가 판단하지 않는다. 어떤 진술이 열려 있다, 미해결이다, 증명되지 않았다고 쓰는 것은 논문이나 교과서가 그렇게 명시한 경우뿐이고, 그 출처를 각주로 단다. 절 이름은 "알려진 열린 문제" 로 쓴다. "남은 방향", "이어지는 방향", "후속 방향", "남은 문제", "남은 어려움", "전망" 같은 절은 문서를 쓰는 사람의 추측이므로 두지 않는다. 명명된 추측(BSD 추측, Vandiver 추측)의 이름과 진술은 열린 문제 언급이 아니라 정의이므로 각주 없이 써도 된다. "대부분 열려 있다", "일부는 여전히 열려 있다", "대체로 열려 있다" 처럼 범위를 뭉뚱그린 문장은 출처가 있어도 쓰지 않고, 어느 진술이 열려 있는지 하나씩 쓴다. 후보는 다음으로 찾는다.
  ```bash
  grep -nE '^## (남은|이어지는|후속|관련|앞으로의) (방향|문제|어려움|위험|과제)|^## (전망|열린 문제|미해결 문제) *$' docs/*.md
  grep -nE '(대부분|일부는|대체로) (열려|열림|미해결)' docs/*.md
  ```
- 문서를 보강할 때 이 규칙에 어긋나는 헤딩과 문단이 보이면 그 문서 전체를 이 문체로 고친다. 새 문서는 처음부터 이 문체로 쓴다.

### 제목과 파일명

- 제목은 문서가 다루는 개념 하나이고, 파일명은 그 개념의 영문 id 다. 둘이 같은 것을 가리켜야 한다. `topology.md` 의 제목이 "위상 공간" 이면 맞고 "위상수학" 이면 틀리다. 분야 전체는 개관 문서의 몫이다.
- "A와 B" 꼴 제목은 두 주제를 한 문서에 담았다는 신호다. 파일명이 그중 하나만 가리키면 어긋난 것이다. 문서를 보강할 때 제목을 파일명의 개념으로 좁히거나, 파일명을 바꾸거나, 8절대로 두 문서로 나눈다. 정리 이름과 그 정리가 답하는 개념을 나란히 둔 제목("Néron–Tate 높이와 Mordell–Weil 정리")은 한 주제이므로 그대로 둔다.
- 파일명을 바꿀 때는 `git mv` 한 뒤 `grep -rn "old-id.md" docs README.md` 로 모든 참조를 새 id 로 고치고 `validate` 를 통과시킨다.

### 수식

- 문서를 읽는 렌더러는 MathJax 다. 마크다운을 먼저 파싱하고 그 결과 문자열을 MathJax 에 넘기므로, 마크다운이 수식을 건드리면 그 자리에 `Extra open brace or missing close brace` 같은 오류가 찍히고 식이 통째로 사라진다. 아래 규칙은 전부 마크다운이 수식을 건드리지 못하게 하는 것이다. KaTeX 만 통과시키는 검사로는 이 오류를 잡지 못한다.
- 수식은 전부 LaTeX 로 쓴다. 따로 떼어 보여야 하는 식은 `$$` 블록에, 문장 안에 섞이는 식은 인라인 `$...$` 에 넣는다. 인라인 코드블록에 수식을 넣지 않는다. 백틱 안의 `B_{n,\chi}` 는 그대로 노출되어 읽기 어렵고, 인라인 수식 $B_{n,\chi}$ 는 제대로 렌더링된다. 닫는 `$` 뒤에는 공백 1개가 반드시 따라와야 한다.
- 렌더러는 수식보다 마크다운 강조를 먼저 읽는다. GitHub 도, markdown-it 도 그렇다. 그래서 한 문단 안에 `$\mathrm{GL}_n$` 과 `$\Pi_\varphi$` 가 함께 있으면 첫 `_` 가 기울임을 열고 둘째 `_` 가 닫아, 그 사이가 통째로 기울임이 되고 두 수식이 깨진다. `_` 앞이 `}` `)` `]` `|` 같은 문장부호이면 열림 후보이고, `_` 뒤가 `\` `{` `(` 이면 닫힘 후보다. 앞뒤가 모두 영숫자인 `x_i`, `a_1` 은 안전하다.
- 그러므로 인라인 수식에서 첨자 `_` 의 바로 앞이 영숫자가 아니면 `\_` 로 쓴다. `$\mathrm{GL}\_n$`, `$\lbrace x_i\rbrace\_{i\in I}$`. 마크다운이 `\_` 를 `_` 로 풀어 수식 엔진에 넘기므로 렌더링은 같다. 첨자 앞이 영숫자인 `$\Pi_\varphi$` 는 그대로 둔다.
- 같은 이유로 수식 안에서는, 인라인이든 `$$` 블록이든, 백슬래시 뒤에 ASCII 문장부호가 오는 LaTeX 명령을 쓰지 않는다. 마크다운이 `\_` 를 푸는 것과 똑같이 `\{` 를 `{` 로, `\,` 를 `,` 로, `\#` 를 `#` 으로, `\%` 를 `%` 로, 행렬의 `\\` 를 `\` 로 풀어 수식 엔진에 넘기기 때문이다. 집합 `$\{2,5,7\}$` 이 그룹 괄호로 먹혀 중괄호 없이 `2,5,7` 로 보였던 사고가 이것이다. `\_` 는 풀려도 결과가 같아서 규칙이 되었지만, 그 밖의 이스케이프는 풀리면 뜻이 바뀐다. 대신 글자로 된 명령을 쓴다.
  | 쓰지 않는다 | 대신 | 비고 |
  | --- | --- | --- |
  | `\{` `\}` | `\lbrace` `\rbrace` | `\left\lbrace`, `\right\rbrace` 도 같다 |
  | `\,` `\:` `\;` `\!` | `\thinspace` `\medspace` `\thickspace` `\negthinspace` | |
  | `\#` | `\char35{}` | 원소 개수 `\char35{}E(\mathbb F_p)` |
  | `\%` | 수식 밖 텍스트 `15%` | 수식 안에 남겨야 하면 `\char37{}` |
  | 행렬의 `\\` | `\cr` | `\begin{pmatrix}a&b\cr c&d\end{pmatrix}` |
  | `\|` | `\Vert` | 표 셀 안에서도 같다. 단선은 `\vert` |
  명령 뒤에 영숫자가 오면 공백을 하나 둔다. `$\lbrace 2,5,7\rbrace$`, `$\Vert f\Vert\_2$`. `$$` 블록도 예외가 아니다. GitHub 은 여러 줄 `$$` 블록 안의 `\{\varnothing\}` 도 `{\varnothing}` 으로 풀어 중괄호 없이 그렸다. 블록 안 행렬과 `align` 의 줄바꿈 `\\` 도 `\cr` 로 쓴다.
- 인라인 수식 안의 `*` 는 `\ast` 로 쓴다. `$T^\ast$`, `$X\_\ast(T)$`.
- 수식 줄을 백슬래시로 끝내지 않는다. 줄 끝 백슬래시는 마크다운의 강제 줄바꿈이라 백슬래시가 사라지고 `<br>` 가 식 안에 박혀 그 블록이 깨진다. 여백은 `\ ` 대신 `\thinspace` 로 쓴다. `\thinspace` 는 줄 어디에 와도 안전하다.
- GitHub 의 수식 렌더러가 허용하지 않는 매크로를 쓰지 않는다. `\operatorname` 은 "The following macros are not allowed: operatorname" 오류로 수식 전체가 깨진다. 글자 연산자는 `\mathrm` 으로 쓰고, 첨자를 아래에 붙이는 `\operatorname*` 은 `\mathop{\mathrm{...}}` 으로 쓴다. `\bm` 은 `\boldsymbol` 로 쓴다. `\DeclareMathOperator`, `\newcommand`, `\def` 도 쓰지 않는다. 다음이 아무것도 내놓지 않아야 한다.
  | 쓰지 않는다 | 대신 |
  | --- | --- |
  | `\operatorname{tr}` | `\mathrm{tr}` |
  | `\operatorname*{Res}_{s=1}` | `\mathop{\mathrm{Res}}\_{s=1}` |
  | `\bm{x}` | `\boldsymbol{x}` |
  ```bash
  grep -nE '\\(operatorname|bm[^a-zA-Z]|DeclareMathOperator|newcommand|renewcommand|def[^a-zA-Z])' docs/*.md
  ```
- 수식 안의 부등호 `<` `>` 는 `\lt` `\gt` 로 쓴다. `<` 뒤에 영문자가 오면 렌더러가 HTML 태그의 시작으로 읽어 그 뒤를 통째로 삼키고, 열려 있던 `{` 가 짝을 잃어 `Extra open brace or missing close brace` 가 뜬다. `\min\_{0\le k<K}` 가 그렇게 깨졌다. `\lt` 뒤에 영숫자가 오면 공백을 하나 둔다. `$0\lt \eta\lt 2/L$`, `$\gamma\gt 1$`. markdown-it 은 `<K}` 를 무효 태그로 보아 그냥 두므로 `render-check` 가 이것을 잡지 못한다.
- 표 셀 안의 수식에는 `|` 문자를 쓰지 않는다. 셀 구분자로 읽히기 때문이다. 단선은 `\vert` 나 `\mid`, 이중선은 위 표대로 `\Vert` 다. `| $e^{-2\pi\Vert z\Vert^2}$ |`. 표 밖에서도 같은 명령을 쓰면 표로 옮길 때 고칠 것이 없다.
- `$$` 블록도 강조를 피하지 못한다. 블록 안의 내용은 마크다운에게 그냥 문단이어서, `_` 앞이 영숫자가 아닌 것이 한 블록에 둘 있으면 그 사이가 기울임이 되고 식이 깨진다. 인라인과 같은 규칙으로 `\_` 를 쓴다. `\mathrm{Hom}\_{\mathcal C}`, `\mathrm{Inf}\_i`. 블록 안의 줄이 `-`, `*`, `+`, `#`, `>`, `1.` 로 시작하면 리스트나 헤딩으로 읽히니 `{-}` 처럼 감싼다.
- 이 규칙은 마크다운을 먼저 파싱하는 렌더러를 대상으로 한다. 수식을 먼저 잘라내는 렌더러로 바꾸는 날이 오면 `\_` 를 일괄 `_` 로 되돌리면 된다.
- 규칙을 외우는 것보다 검수가 확실하다. 커밋 전에 고친 문서마다 다음 다섯 가지를 전부 통과시킨다. 하나라도 출력이 있으면 커밋하지 않는다. `render-check` 만 믿으면 안 된다. 이스케이프가 풀린 결과도 원문과 같은 것으로 보아 위 표의 `\{` `\,` 를 잡지 못하고, 부등호, 줄 끝 백슬래시, `$$` 블록 안의 밑줄도 잡지 못한다. 이 검사를 빠뜨려서 집합 기호 `\{` `\}` 가 문서 300여 개에서 사라졌고, 경사하강법 문서의 수렴률 블록은 `k<K` 의 부등호 하나 때문에 식 대신 오류 문구를 띄운 채 여러 회차를 넘겼다.
  ```bash
  awk '/^\s*(```|~~~)/{f=!f} f{next} /^\s*>?\s*\$\$\s*$/{b=!b; next} (b || /\$/) && /\\[!"#$%&()*+,.\/:;<=>?@[\\\]^`{|}~-]/{print FILENAME":"FNR": "$0}' docs/<id>.md
  grep -n '<[a-zA-Z]' docs/<id>.md               # 태그로 읽히는 부등호
  grep -n '\\$' docs/<id>.md                    # 줄 끝 백슬래시
  grep -noE '[^a-zA-Z0-9\\]_' docs/<id>.md       # 이스케이프 안 된 밑줄. 인라인과 $$ 블록 모두
  node dev/render-check.mjs <id>                 # 수식 스팬이 렌더 결과에 남는지
  ```

### 코드

- 코드블록의 언어는 JavaScript 나 TypeScript 만 허용한다. Python 을 비롯한 다른 언어의 코드블록은 쓰지 않는다. 있는 것은 지우거나 JavaScript/TypeScript 로 옮긴다. 언어를 표시하지 않은 코드블록과 `text` 블록도 쓰지 않는다. 다음이 아무것도 내놓지 않아야 한다.
  ```bash
  grep -nE '^```[a-z]+' docs/*.md | grep -vE ':```(javascript|typescript|mermaid|js|ts)$'
  for f in docs/*.md; do awk '/^```/{ if(!o){o=1; if($0=="```") print FILENAME": "NR} else o=0 }' "$f"; done
  ```
- 코드는 의사코드다. 알고리즘의 절차를 보이는 데만 쓰고, 실행해서 결과를 얻는 프로그램(라이브러리 호출, 출력문, 실행 결과 주석)을 넣지 않는다. 프로그램의 출력을 옮긴 블록도 넣지 않는다.
- 코드는 개념 자체가 계산 절차일 때 넣는다. 유클리드 알고리즘, 동적 계획법, Sinkhorn 반복, LLL 축소, 부호의 복호, Hensel 올리기처럼 코드가 곧 정의의 실행인 경우다. 증명이 프로그램이 되는 대응을 코드로 옮기는 것도 여기 든다.
- 정리가 맞는지 수치로 다시 확인하는 코드는 넣지 않는다. "반례 0개", "차이 1e-10", "시뮬레이션이 이론과 맞는다" 를 출력하는 절은 문서를 쓰는 사람이 이해하려고 돌린 실험이지 독자의 지식이 아니다. `## 수치로 확인`, `## 시뮬레이션`, `## 계산으로 확인한다` 같은 절이 보이면 지운다. 그 절에 지식이 되는 관찰(예: Gauss 합의 부호가 언제 실수이고 언제 순허수인지)이 섞여 있으면 그 문장만 제목을 바꿔 남긴다.
- 같은 이유로 "직접 확인", "확인해 보자" 같은 표현이 붙은 산문도 검증 과정이면 지운다.

### 다이어그램

- mermaid 는 구조를 그릴 때만 쓴다. 문서 의존 그래프, Hasse 도형, 확대의 탑, 상태 기계, 오토마타, 가환도, 보편성질의 도형, 실제 그래프의 예처럼 정점과 간선 자체가 내용인 경우다.
- 문장으로 쓸 수 있는 것을 화살표로 늘어놓지 않는다. "가정 P → 결론 Q", 증명의 단계 나열, 세 개념의 비교, 정의의 사슬은 다이어그램이 아니라 문장이나 표다. 다이어그램의 정점을 빼고 문장 하나로 쓸 수 있으면 다이어그램을 지운다.
- 문서를 보강할 때 이런 나열형 다이어그램이 있으면 지우고, 그림에만 있던 정보(예: 세 이동의 이름)는 목록으로 옮긴다.

### 마크다운

- 마크다운, 언어를 명시한 코드블록, LaTeX, 표를 적극적으로 쓴다.
- 굵은 글씨는 정의되는 용어에만 쓴다.

## 4.5. 분야 개관

분야마다 대시보드 문서를 하나 둔다. id 는 `<분야>-overview`, 제목은 "<분야> 개관" 이다. 지금 있는 것은 `abstract-algebra-overview`, `combinatorics-overview`, `foundations-overview`, `linear-algebra-overview`, `number-theory-overview`, `topology-overview` 다. 문서가 10개 이상인 분야 태그(해석학, 확률론, 계산, 미분기하, 복소해석, 그래프 이론, 범주론, 측도론, 최적화 등)에 개관이 없으면 만드는 것이 새 주제보다 우선이다.

개관의 구조는 4절과 다르다.

```markdown
# 정수론 개관

# 개요        분야가 무엇을 묻는지, 처음 읽는 사람이 어느 문서에서 시작해 어디로 갈라지는지

# 지도        핵심 문서 12~20개의 선수관계를 그린 mermaid DAG. 화살표는 실제 간선만

# 갈래        소분야마다 H2 하나. 문서를 읽는 순서로 나열하고 한 줄씩 설명. 본문 링크라 간선이 아니다

# 연관 문서
## 선수지식   뿌리(sets, proofs) 가운데 하나 또는 둘
## 더 알아보기  분야 진입 문서. 부모가 모두 분야 밖인 문서와 분야를 규정하는 축 문서 몇 개

#분야태그 #overview
```

- 개관에 `# 빈자리` 절을 두지 않는다. 갈래에 있어야 하는데 없는 문서는 큐 항목이다(0절). 갈래에는 실제로 있는 문서만 적는다.
- 개관에서 진입 문서로 가는 간선은 선수관계가 아니라 포함관계다. 개관을 읽지 않아도 진입 문서를 이해할 수 있지만, 개관이 그 문서를 갈래로 거느린다는 뜻으로 긋는다. 진입 문서의 선수지식에 개관을 적을 때 부모가 4개를 넘지 않게 한다.
- 갈래에는 그 분야를 주 분야로 하는 문서, 곧 태그 줄의 첫 태그가 그 분야인 문서를 빠짐없이 넣는다. 다른 분야가 주인 문서는 관련이 깊을 때만 넣는다. 새 문서를 만들면 그 분야 개관의 갈래에 한 줄을 더한다. 빠진 문서는 다음으로 찾는다.
  ```bash
  for f in docs/*.md; do id=$(basename $f .md); [ "$(tail -1 $f | grep -o '#[a-z0-9_]*' | head -1)" = "#number_theory" ] && ! grep -q "($id.md)" docs/number-theory-overview.md && echo $id; done
  ```
- 지도의 mermaid 는 실제 간선의 부분그래프여야 한다. 문서를 보강하다 간선을 바꿨으면 개관의 지도도 맞춘다.
- 개관은 README 의 "지도의 갈래" 절에 id 순으로 나열한다. 규칙은 7절.

## 5. 간선 규칙

- 간선 방향은 선수지식에서 후속으로 간다. 부모 문서를 읽지 않으면 자식 문서를 이해할 수 없어야 한다.
- 간선은 선수관계, 특수화, 포함관계를 반영한다. 일반 개념이 특수 사례의 부모가 되고, 도구가 그 도구를 쓰는 정리의 부모가 되고, 개관이 갈래의 부모가 된다.
- 루트(선수지식 없음)는 지금 `sets` 와 `proofs` 뿐이다. 새 루트를 만들지 않는다.
- 부모는 보통 1개에서 3개다. 4개 이상이면 그 중 다른 부모의 조상인 것을 뺀다.
- 왜 그 부모인지가 자식 문서의 `# 개요` 나 `# 직관` 에서 드러나야 한다.

### 더 알아보기의 기준

더 알아보기는 DAG 의 자식 목록을 기계적으로 옮겨 적는 자리가 아니다. 그 문서의 주제에서 더 깊이 들어가는 방향(심화)이거나, 그 주제가 부분으로 거느리는 방향(has)일 때만 넣는다. "이 문서를 읽을 줄 알아야 저 문서를 읽는다" 는 정도의 관계는 간선이 아니다. 명제와 증명이 벡터 공간이나 군의 부모였던 것이 그런 예이고, 그 간선은 지웠다. 다음을 자문한다.

- 자식이 부모 주제의 특수 사례, 확장, 응용, 부분인가. 아니면 그저 부모를 도구로 한 번 쓰는가. 후자는 자식의 본문 링크로 충분하다.
- 부모를 빼도 자식의 개요가 그대로 읽히는가. 그렇다면 그 부모는 간선이 아니라 배경이다.
- 자식을 부모의 더 알아보기에서 봤을 때 "이 주제를 더 알고 싶으면 다음은 이것" 이라는 안내가 되는가.

### 더 알아보기의 묶음과 정렬

- 자식이 6개 이상이면 `### 묶음 이름` H3 로 나눈다. 묶음은 주제(예: "구조와 알고리즘", "대수와 위상") 또는 관계(예: "특수 사례", "다른 분야에서의 쓰임")로 짓는다. 파서는 H3 를 무시하고 링크 줄만 읽으므로 간선에 영향이 없다. H2 는 쓰지 않는다. `## 더 알아보기` 절의 끝으로 읽힌다.
- 묶음 안에서는 선수관계 순으로 정렬한다. 자식 A 가 자식 B 의 조상이면 A 가 앞이다. 조상관계가 없는 것끼리는 가까운 후속(바로 다음 단계)을 먼 후속보다 앞에 둔다. `node dev/order-check.mjs` 가 조상이 뒤에 온 곳을 잡는다.
- 자식이 5개 이하면 묶지 않고 정렬만 한다.
- 새 자식을 더할 때 목록 끝에 붙이고 끝내지 않는다. 어느 묶음에 들어가는지 정하고 그 자리에 넣는다.

## 6. 태그 규칙

- 태그는 `#` 뒤에 영소문자, 숫자, 언더스코어가 이어진 단어다. 예: `#category_theory`, `#probability`, `#computation`.
- 문서 마지막 줄에 공백으로 구분해 한 줄로 쓴다.
- 새 태그를 만들기 전에 `node dev/graph.mjs tags` 로 기존 태그를 보고 재사용한다.
- 태그 줄의 첫 태그가 주 분야다. 분야 개관의 갈래는 이 첫 태그로 문서를 거둔다.
- 분야 태그는 하나가 아니라 관련된 것을 전부 붙인다. 주 분야 하나에, 본문의 정의나 활용 절에서 실제로 쓰는 도구의 분야와 응용되는 분야를 더한다. Radon–Nikodym 정리는 측도론 문서이지만 확률밀도, 우도비, 조건부 기댓값을 정의하는 데 쓰이므로 `#measure_theory #probability #statistics` 다. 부모 문서들의 분야 태그와 활용 절에서 언급하는 분야가 후보다. 문서당 분야 태그 2~4개가 보통이고, 보강하거나 새로 쓰는 문서의 태그가 없거나 하나뿐이면 채운다.
- 성격 태그(`#theorem`, `#construction`, `#example`, `#paper`, `#overview`)는 필요할 때만 붙인다.

## 7. README 유지

루트 `README.md` 는 저장소의 첫 화면이다. "지도의 뿌리" 절은 그래프의 minimal 요소, 곧 `node dev/graph.mjs roots` 가 내놓는 문서 전부를 담는다. README 의 링크는 DAG 간선이 아니어서 `validate` 가 잡아주지 않으므로 2절 9단계의 diff 로 매 회차 확인한다.

5절에 따라 새 루트는 만들지 않으므로 이 목록은 평소에 움직이지 않는다. 문서를 지우거나 간선을 고치다가 어떤 문서의 선수지식이 통째로 사라지면 그 문서가 조용히 minimal 로 올라오고, 이는 대개 실수다. diff 에 새 id 가 뜨면 README 에 추가하기 전에 그 문서의 선수지식부터 복구할지 판단한다.

- "지도의 갈래" 절은 분야 개관 전부를 id 순으로 담는다. 개관을 만들면 여기에 더한다. 이 절은 "지도의 뿌리" 절보다 앞에 있어야 한다. 위 diff 가 뿌리 절 이후의 링크를 전부 읽기 때문이다.
- 링크 형식은 `- [문서 제목](docs/<id>.md)` 다. `docs/` 접두사가 붙는 점이 문서끼리의 링크와 다르다. README 가 저장소 루트에 있기 때문이다.
- 목록은 id 순으로 정렬한다. 항목이 적어 분야로 묶지 않는다.
- "구조" 절의 문서 수, 간선 수, 태그 수는 문서를 더하거나 지운 회차마다 맞춘다. `node dev/graph.mjs nodes`, `edges`, `tags` 의 마지막 줄이 각각의 개수를 알려준다.

## 8. 통합과 분할

문서는 흩어지기 쉽고 스스로 모이지 않는다. 회차마다 하나씩 쓰다 보면 같은 개념이 두 문서에 나뉘고, 한 분야의 문서들이 서로를 모른 채 놓인다. 다음 기준으로 모은다.

- 두 문서가 서로 없이는 설명되지 않고, 한쪽의 더 알아보기가 다른 쪽뿐이며, 합쳐도 1000줄 미만이면 합친다. 수학적 직관주의(철학)와 직관주의 논리(형식 체계)가 그런 경우였고 `intuitionism` 하나로 합쳤다. 같은 개념을 "X" 와 "X 의 이론", "X" 와 "X 의 의미론" 으로 나눠 놓은 쌍이 후보다.
- 합칠 때는 새 id 와 제목을 4절의 제목 규칙대로 정하고, 두 문서의 부모와 자식을 새 문서로 옮기고, `grep -rn "old-id.md" docs README.md` 로 본문 링크를 전부 바꾸고, 옛 파일을 `git rm` 한다. 중복 절(같은 표, 같은 증명)은 하나만 남긴다.
- 같은 분야의 문서 여럿이 서로 이웃이 아니면(수리철학의 다섯 입장처럼) 그 분야 개관의 갈래에 한 줄로 세우고, 개관에서 각 문서로 간선을 긋는다. 문서를 합치는 것과 개관 아래 모으는 것은 다른 일이다. 각자 독립된 개념이면 합치지 않고 모은다.
- 한 문서가 1000줄을 넘거나 제목이 "A와 B" 인데 A 와 B 가 각자 문서가 될 만하면 나눈다. 나눈 뒤 두 문서 사이의 선수관계를 긋고, 원래 문서를 가리키던 링크를 맞는 쪽으로 돌린다.
- 통합이나 분할을 했으면 커밋 메시지에 옛 id 와 새 id 를 적는다. 다른 세션이 pull 받고 상황을 알 수 있게 한다.

## 9. 스크립트 요약

| 명령                                                            | 용도                              |
| --------------------------------------------------------------- | --------------------------------- |
| `node dev/graph.mjs node <id>`                                  | 문서의 제목, 태그, 부모, 자식     |
| `node dev/graph.mjs check-cycle <parent> <child> [--allow-new]` | 간선 추가 시 순환 검사            |
| `node dev/graph.mjs validate`                                   | 깨진 링크, 비대칭 간선, 파싱 문제 |
| `node dev/graph.mjs cycles` / `topo`                            | 순환 목록 / 위상순                |
| `node dev/graph.mjs roots` / `leaves` / `isolated`              | 구조 파악                         |
| `node dev/graph.mjs ancestors <id>` / `descendants <id>`        | 조상 / 후손                       |
| `node dev/graph.mjs tags [untagged \| <tag>]`                   | 태그 통계                         |
| `node dev/render-check.mjs [id ...]`                            | 렌더링에서 깨지는 수식 검수       |
| `node dev/order-check.mjs`                                      | 더 알아보기 순서 검사             |
| `node dev/queue.mjs push "<title>" -p N -n "<note>"`            | 탐사 대기열에 추가                |
| `node dev/queue.mjs pop` / `peek` / `list` / `remove <id>`      | 대기열 조작                       |
| `node dev/queue.mjs stop`                                       | 큐 저장, 커밋, 서버 종료          |
| `node dev/timer.mjs start` / `check`                            | 회차 타이머                       |
| `node --test dev/*.test.mjs`                                    | 스크립트 자체 테스트              |

모든 명령은 `--json` 을 받는다. `node dev/graph.mjs help`, `node dev/queue.mjs help`, `node dev/render-check.mjs help`, `node dev/order-check.mjs help` 로 전체 목록을 볼 수 있다.
